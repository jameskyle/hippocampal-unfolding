%
%  count = writecap(filename,data)
%    -- Write the matrix data into a Cap Matrix file named filename.
%
%  data = readcap((string) filename, (string) filetype)
%	-- Read a matrix for a filename and a filetype.
%	The type should be one of
%	  'CapMatrix'   'CapBasis'   'CapImage'
%
